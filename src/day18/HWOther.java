package day18;

public class HWOther {
    /*public static void main(String[] args) {
        //1.узнать площадь стены или пола
        //2.узнать кол-во плитки на 1 кв.м в двух вариантах
        //3.узнать общее кол-во плитки
        //4.узнать стоимость работы
        //5. узнать примерную общую стоимость. mmm
        double tile1 = numberOfWholeTitle(2, 3);
        double tile2 = numberOfOtherTitle(30, 20);
        double cost1 = costOfTitle();
        costWork(27);
        double cost3 = estimatedTotalCost(1); // 1 square meter in hour
        int leght = 2;
        int width = 3;

        //int area = wallArea(2, 3);

    }

    private static double estimatedTotalCost(int a) {                     //примерная общая стоимость
        return 0;
    }

    private static double costWork(int hourPrice) {              //стоимость рабочего времени
        int cost = hourPrice * wallArea(leght, width);
        System.out.println("стоимость рабочего времени " + cost);
        return cost;
    }

    private static int wallArea(int dlina, int shirina) {          //площадь
        int areaW = dlina * shirina;
        //System.out.println("площадь " + areaW);
        return areaW;
    }

    private static double costOfTitle() {                            // стоимость  плитки
        return 0;
    }

    private static double numberOfOtherTitle(int lenghtW, int widthW) {         // кол-во плитки для резки
        double lenghtTitle = 0.30;
        double widthTitle = 0.20;
        double result3 = lenghtW / lenghtTitle;
        double result4 = widthW / widthTitle;
        double restL =
                System.out.println();
        return 0;
    }


    private static double numberOfWholeTitle(int lenghtW, int widthW) {         //кол-во плитки целой
        double lenghtTitle = 0.30;
        double widthTitle = 0.20;
        double result1 = Math.ceil(lenghtW / lenghtTitle);
        double result2 = Math.ceil(widthW / widthTitle);
        System.out.println("Сколько плиток в длину " + result1);
        System.out.println("Сколько плиток в высоту " + result2);

        return 0;
    }*/
}
